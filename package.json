{
  "name": "benzed-mono",
  "version": "0.0.1",
  "description": "Mono repo for all of BenZeds util and helper classes for scoped npm distribution.",
  "main": "index.js",
  "scripts": {
    "test": "lerna run test --parallel",
    "build:dev": "lerna run build:dev --parallel",
    "webpack:dev": "lerna run webpack:dev --parallel",
    "serve:dev": "lerna run serve:dev --parallel",
    "update": "bash update-deps.sh"
  },
  "author": "BenZed",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^2.9.1",
    "webpack-dev-server": "^3.1.1"
  },
  "dependencies": {
    "react": "^16.3.1"
  }
}
