// import { expect } from 'chai'

// eslint-disable-next-line no-unused-vars
/* global describe it before after beforeEach afterEach */

describe('run', () => {

  it('is a function')

  it('needs to be bound to an app constructor')

  it('takes a config as argument')

  it('configures app, exits process on failure')

  it('initializes app, exits process on failure')

  it('starts app, exits process on failure')

})
