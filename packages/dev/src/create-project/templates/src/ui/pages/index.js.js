
export default ({ ui, routing, pretty }) => ui && routing && pretty`
import Home from './home'
import Missing from './missing'

/******************************************************************************/
// Exports
/******************************************************************************/

export { Home, Missing }
`
